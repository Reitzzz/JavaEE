server:
  port: 8080

spring:
  application:
    name: smart-library
  datasource:
    url: ${DB_URL:jdbc:mysql://localhost:3306/smart_library?createDatabaseIfNotExist=true&useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai&useSSL=false&allowPublicKeyRetrieval=true}
    username: ${DB_USERNAME:root}
    password: ${DB_PASSWORD:niuniu0626}
    driver-class-name: com.mysql.cj.jdbc.Driver
  sql:
    init:
      mode: always
      encoding: UTF-8

llm:
  base-url: ${LLM_BASE_URL:https://api.openai.com}
  api-key: ${LLM_API_KEY:}
  model: ${LLM_MODEL:gpt-4o-mini}
  chat-path: ${LLM_CHAT_PATH:/v1/chat/completions}
